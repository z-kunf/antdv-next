import type { CSSProperties } from 'vue'

export const iconWrap: CSSProperties = {
  display: 'inline-flex',
  alignItems: 'center',
  lineHeight: 0,
  textAlign: 'center',
  verticalAlign: '-0.125em',
}
